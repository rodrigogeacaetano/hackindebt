android {
            defaultConfig {
                applicationId = "com.hack.debit"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }